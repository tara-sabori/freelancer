import { HashLoader } from "react-spinners";

const Loading = () => {
  return (
    <div className="w-full h-[80vh] flex items-center justify-center">
      <HashLoader color="#4a6dff" />
    </div>
  );
};

export default Loading;
